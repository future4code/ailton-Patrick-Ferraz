export type productsInsert = {
    name: string;
    price: number;
    image_url: string;
}

export type productsData = {
    id: string;
    price: number;
    name: string;
    image_url: string;
}